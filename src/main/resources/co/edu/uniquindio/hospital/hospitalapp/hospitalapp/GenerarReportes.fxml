<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns:fx="http://javafx.com/fxml" fx:controller="co.edu.uniquindio.hospital.hospitalapp.hospitalapp.viewController.GenerarReportesViewController" prefWidth="500" prefHeight="400">
    <children>
        <VBox spacing="10" alignment="TOP_LEFT" AnchorPane.topAnchor="10" AnchorPane.leftAnchor="10" AnchorPane.rightAnchor="10" AnchorPane.bottomAnchor="10">
            <children>
                <HBox spacing="10">
                    <Label text="Médico:"/>
                    <ChoiceBox fx:id="choiceMedico" prefWidth="200"/>
                </HBox>
                <HBox spacing="10">
                    <Label text="Cita:"/>
                    <ChoiceBox fx:id="choiceCita" prefWidth="200"/>
                </HBox>
                <HBox spacing="10">
                    <Label text="Fecha:"/>
                    <DatePicker fx:id="dateFechaCita"/>
                    <Label text="Hora:"/>
                    <TextField fx:id="txtHoraCita" promptText="HH:MM"/>
                </HBox>
                <HBox spacing="10">
                    <Label text="Motivo de la cita:"/>
                    <TextField fx:id="txtMotivoCita" prefWidth="250"/>
                </HBox>
                <HBox spacing="10">
                    <Label text="Estado:"/>
                    <ChoiceBox fx:id="choiceEstadoCita" prefWidth="150"/>
                </HBox>
                <HBox spacing="10" alignment="TOP_LEFT">
                    <Label text="Motivo del cambio:"/>
                </HBox>
                <TextArea fx:id="txtMotivosCambio" prefRowCount="3" prefWidth="400"/>
                <HBox spacing="20" alignment="CENTER">
                    <Button fx:id="btnGuardar" text="Guardar"/>
                    <Button fx:id="btnCancelar" text="Cancelar"/>
                </HBox>
            </children>
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
        </VBox>
    </children>
</AnchorPane>